{
  "name": "cv",
  "version": "1.0.0",
  "private": true,
  "description": "Personal CV site — Hernan von Hausen. Generates HTML pages in English and Spanish from JSON data; source PDFs are committed.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "npx --yes http-server dist -p 8080 -c-1",
    "dev": "npm run build && npm run serve"
  },
  "dependencies": {
    "mustache": "^4.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
